#include <iostream> 
using namespace std;int main (){int initInterval1, endInterval1, initInterval2, endInterval2, number;cin >> initInterval1 >> endInterval1 >> initInterval2 >> endInterval2 >> number;bool inInterval1 = false, inInterval2 = false;if((number > initInterval1) && (number <= endInterval1) ){inInterval1 = true;}if((number >= initInterval2) && (number < endInterval2) ){inInterval2 = true;}if (inInterval1 && inInterval2){cout << "Ambos!" << endl;} else if (!inInterval1 && !inInterval2){cout << "Nenhum!" << endl;} else if (inInterval1 && !inInterval2){cout << "Primeiro intervalo!" << endl;} else if (!inInterval1 && inInterval2){cout << "Segundo intervalo!" << endl;}return 0;}